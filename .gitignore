# Secrets — never commit
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# OS
.DS_Store

# Note: code/cache/media.json is deliberately NOT ignored. It holds the
# one-time OCR/ASR extraction output and is what makes reruns deterministic
# and reproducible without re-calling the vision/ASR providers.
# See DECISIONS.md.
